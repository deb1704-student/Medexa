/**
 * PLACEHOLDER — this file is auto-generated. Do not hand-edit.
 *
 * Once the FastAPI backend is running (see backend/), run:
 *   npm run gen:types
 *
 * This calls `openapi-typescript http://localhost:8000/openapi.json`
 * and overwrites this file with real types generated directly from the
 * backend's Pydantic models. That's the mechanism that makes frontend/
 * backend drift a compile error instead of a runtime surprise
 * (Build Guide Section 7, "API type-safety bridge").
 *
 * Until the backend exists, src/models/careEpisode.ts (hand-written Zod
 * schemas) is the source of truth for the frontend team to build against.
 */
export type components = Record<string, never>;
export type paths = Record<string, never>;
